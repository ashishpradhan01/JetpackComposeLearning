<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.prime.jetpackcomposelearning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackComposeLearning">
        <activity
            android:name=".ManageStateActivity"
            android:exported="true"
            android:label="@string/title_activity_manage_state"
            android:theme="@style/Theme.JetpackComposeLearning.NoActionBar">
        </activity>
        <activity
            android:name=".ImageCardAndTextPlayActivity"
            android:exported="true"
            android:label="@string/title_activity_image_and_text_play"
            android:theme="@style/Theme.JetpackComposeLearning.NoActionBar"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackComposeLearning.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackComposeLearning.NoActionBar"/>

        <activity
            android:name=".ListActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackComposeLearning.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>